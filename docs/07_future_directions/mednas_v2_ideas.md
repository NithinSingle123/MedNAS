- What if i select which family of loss function this problem belongs to and then use if else or try catch for it to switch loss functions accordingly and this case should be used for multiple batches if they exist

- and we wont be doing the decision here we would input to get some parameter outputs to decide which loss fn we are going to use, depending on parameters like variance, and stuff